from distutils.core import setup

setup(name='minisam',
      version='0.0.0',
      packages=['minisam'],
      package_dir={'minisam': '${MINISAM_WRAPPER_PATH}'},
      package_data={'minisam': ['${MINISAM_WRAPPER_FILE_NAME}', 'libminisam*', 'minisam*']})
